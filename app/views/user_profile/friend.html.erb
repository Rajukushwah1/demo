<% @totle_friends.each do |friend| %>
    <% if (friend.confirmation == true) %>
        <strong style="color:#666600"><% user = User.find(friend.sender_id) %>
        (<%= user.id %>)<%= user.name %></strong>     <%= link_to 'Destroy', friendshipe_path(friend),
              method: :delete,
              data: { confirm: 'Are you sure?' }, class:"btn btn-primary" %><br> 

    <% end %>
<% end %><br>
