<h1>Friend request aceapt</h1>

<% Friendship.all.each do |x| %>
	<% if x.confirmation == true %>
	  <%= x.confirmation %><br>
	<% end %>

<% end %>
<% User.all.each do |user| %>
     <%= user.id %><br>

	 <%= user.name %><br>
	
	         <%= button_to " accept request", friendshipes_path(sender_id: current_user.id, receiver_id: user.id) %> 
<% end %>